// import { getCookieToDocument } from '@utils/cookie/client';
// import { getCookie } from '@utils/cookie/server';

// const TOKEN_COOKIE_NAME = 'token';

// const getTokenCookie = async () => {
//   if (document) {
//     return getCookieToDocument(TOKEN_COOKIE_NAME);
//   }

//   return await getCookie(TOKEN_COOKIE_NAME);
// };

// export default getTokenCookie;
